language: node_js
sudo: false
node_js:
  - '8'
  - '10'
services:
  - docker
env:
  - CI=true
## Cache NPM folder and Cypress binary
cache:
  directories:
    - ~/.npm
    - ~/.cache
install:
  - npm ci
